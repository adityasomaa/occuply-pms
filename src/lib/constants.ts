/** Shared with client components, so this file must stay free of server-only
 *  imports such as next/headers. */
export const PROPERTY_COOKIE = "occuply.property"
export const CUSTOM_PROPERTY_COOKIE = "occuply.custom"
